import React from "react";
import ReactDOM from "react-dom";
import reportWebVitals from "./reportWebVitals";

ReactDOM.render(
  <React.StrictMode>
    <h1>🎉🎉TEBRIKLƏR PROQRAMI BAŞLATDINIZ 🚀🚀</h1>
  </React.StrictMode>,
  document.getElementById("root")
);

reportWebVitals();
